const saldo = 1;

const unidade = saldo === 1 ? "real" : "reais";

console.log(`Você possui ${saldo} ${unidade}.`)

// if (saldo === 1) {

//     console.log("Você possui 1 real.")

// } else {
//     if (saldo < 1) {

//         console.log(`Você possui ${saldo.toFixed(2)} centavos.`)

//     } else {

//         console.log(`Você possui ${saldo} reais.`)

//     }

// }
